/// 비회원 테이블
///
/// @namespace Blog
model Guest {
  /// Pirmary Key
  id       Int      @id @default(autoincrement())

  /// 비회원 id, 프론트에서 생성
  guestId  String   @unique

  /// ---
  /// RELATIONS
  /// ---
  postLikes PostLike[]
  guestComments GuestComment[]
}
